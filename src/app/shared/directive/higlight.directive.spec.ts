import { HiglightDirective } from './higlight.directive';

xdescribe('HiglightDirective', () => {
  it('should create an instance', () => {
    //const directive = new HiglightDirective();
    //expect(directive).toBeTruthy();
  });
});
